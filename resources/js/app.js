require("./bootstrap");

import Vue from "vue";

import App from "./vue/app";

const app = new Vue({
    el: "#app",
    components: { App },
});
(function (window) {
    "use strict";

    // Your starting point. Enjoy the ride!
})(window);
